import { GoogleAnalyticsDirective } from './google-analytics.directive';

describe('GoogleAnalyticsDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleAnalyticsDirective();
    expect(directive).toBeTruthy();
  });
});
